# Feature planning and implementation prompt

```text
Problem:
[paste problem description here]

Clarify the desired behavior, constraints, and material unknowns from the request and relevant code. Compare alternatives only when they affect the outcome; explain the selected approach and its failure cases.

If implementation is requested, carry it through integration and relevant verification. Delegate independent work when useful and supported. Research externally only when local evidence is insufficient.

Follow AGENTS.md, including the required Git wrappers. Preserve unrelated changes. Commit/push or create a proposal only when included in the requested deliverable; use the matching skill for those workflows. Report results and unresolved decisions.
```
