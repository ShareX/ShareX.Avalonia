# Implement XIP end to end

```text
Implement these XIPs:
- [path/to/XIP####-name.md]

Follow AGENTS.md, including required Git wrappers and the current-branch rule. Read the selected proposals and inspect the relevant existing implementation, callers, UI, platform services, and tests.

Make a short implementation plan and continue through the complete requested behavior. Use existing boundaries; integrate overlapping proposals. Every advertised UI control must work, and relevant CLI/MCP/workflow surfaces must use the same underlying behavior.

Handle persistence, failures, compatibility, and privacy-sensitive defaults. Do not add cloud behavior unless the requested feature includes it. Preserve unrelated changes.

Run focused tests and the required build. Check UI bindings and command routing where affected. Report shipped behavior, changed files, verification results, and remaining work.

Prepare an implementation summary with evidence-backed acceptance criteria and remaining work. Update local documentation where appropriate. XIP issue edits, comments, closure, and sync require an explicit request; use sync-xips when authorized.

If commit/push is requested, use git-workflow for verified logical batches and report commit hashes and the target branch. Do not require a clean working tree when unrelated user changes remain.
```

Replace bracketed placeholders before use.
